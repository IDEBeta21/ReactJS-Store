services:
  react-vite-store:
    image: react-vite-store
    container_name: react-vite-store
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: dev
      PORT: 8001
    ports:
      - 8001:8001
